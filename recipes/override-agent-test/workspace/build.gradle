import de.undercouch.gradle.tasks.download.Download

task copyOverrideAgent(type: Copy) {
    from new File(projectDir, 'liferay-dxp-agent-jakarta-1.0.3.jar')
    into new File(gradle.liferayWorkspace.homeDir, '/osgi/modules/')
}

deploy {
    dependsOn copyOverrideAgent
}

